version: "3.8"

services:
  websocket-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8765:8765"
    env_file:
      - .env
    # Run with memory commands enabled for benchmarking
    command: python -u run_server.py --enable-memory-commands
